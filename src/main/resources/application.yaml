server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    root: warn
    org:
      springframework:
        web: debug
        data:
          rest: debug

      kafka:
        clients:
          producer: debug
          consumer: debug

spring:
  application:
    name: insight

  datasource:
    username: ${DB_USER}
    password: ${DB_PASS}
    url: jdbc:postgresql://${DB_HOST}/${DB_DATABASE}?stringtype=unspecified

  web:
    error:
      include-message: always
      include-binding-errors: always

  jackson:
    time-zone: UTC
    mapper:
      "[ACCEPT_CASE_INSENSITIVE_ENUMS]": true

  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: validate

  kafka:
    bootstrap-servers:
      - ${KAFKA_HOST}

    consumer:
      value-deserializer: org.springframework.kafka.support.serializer.JacksonJsonDeserializer
      properties:
        spring:
          json:
            trusted:
              packages: edu.raijin.commons.infrastructure.adapter.messaging.event.*

security:
  rsa:
    public-key: classpath:certs/public-key.pem

kafka:
  topics:
    project-commands:
      topic: project-commands
      properties: \
        spring.json.value.default.type=edu.raijin.commons.infrastructure.adapter.messaging.event.scrum.ProjectEvent,\
        enable.auto.commit=false\
        auto.offset.reset=earliest

    sprint-commands:
      topic: sprint-commands
      properties: \
        spring.json.value.default.type=edu.raijin.commons.infrastructure.adapter.messaging.event.scrum.SprintEvent,\
        enable.auto.commit=false\
        auto.offset.reset=earliest

    story-commands:
      topic: story-commands
      properties: \
        spring.json.value.default.type=edu.raijin.commons.infrastructure.adapter.messaging.event.scrum.StoryEvent,\
        enable.auto.commit=false\
        auto.offset.reset=earliest

    user-commands:
      topic: user-commands
      properties: \
        spring.json.value.default.type=edu.raijin.commons.infrastructure.adapter.messaging.event.identity.UserEvent,\
        enable.auto.commit=false\
        auto.offset.reset=earliest

    member-commands:
      topic: member-commands
      properties: \
        spring.json.value.default.type=edu.raijin.commons.infrastructure.adapter.messaging.event.scrum.MemberEvent,\
        enable.auto.commit=false\
        auto.offset.reset=earliest

    contract-commands:
      topic: contract-commands
      properties: \
        spring.json.value.default.type=edu.raijin.commons.infrastructure.adapter.messaging.event.finance.ContractEvent,\
        enable.auto.commit=false\
        auto.offset.reset=earliest

    bonus-commands:
      topic: bonus-commands
      properties: \
        spring.json.value.default.type=edu.raijin.commons.infrastructure.adapter.messaging.event.finance.BonusEvent,\
        enable.auto.commit=false\
        auto.offset.reset=earliest

    discount-commands:
      topic: discount-commands
      properties: \
        spring.json.value.default.type=edu.raijin.commons.infrastructure.adapter.messaging.event.finance.DiscountEvent,\
        enable.auto.commit=false\
        auto.offset.reset=earliest

    expense-commands:
      topic: expense-commands
      properties: \
        spring.json.value.default.type=edu.raijin.commons.infrastructure.adapter.messaging.event.finance.ExpenseEvent,\
        enable.auto.commit=false\
        auto.offset.reset=earliest

    income-commands:
      topic: income-commands
      properties: \
        spring.json.value.default.type=edu.raijin.commons.infrastructure.adapter.messaging.event.finance.IncomeEvent,\
        enable.auto.commit=false\
        auto.offset.reset=earliest

    payroll-commands:
      topic: payroll-commands
      properties: \
        spring.json.value.default.type=edu.raijin.commons.infrastructure.adapter.messaging.event.finance.PayrollEvent,\
        enable.auto.commit=false\
        auto.offset.reset=earliest

    suspension-commands:
      topic: suspension-commands
      properties: \
        spring.json.value.default.type=edu.raijin.commons.infrastructure.adapter.messaging.event.finance.SuspensionEvent,\
        enable.auto.commit=false\
        auto.offset.reset=earliest
